# program to check a number is positive,negative or zero

num = int(input("Enter the number: "))

if num > 0:
    print(f"number is postive {num}")
elif num <0:
    print(f"number is negative {num}")
else :
    print(f"number is zero or invlalid {num}")
